[pytest]
# Monorepo pytest config: points pytest at the FastAPI backend's tests.
# pytest's rootdir auto-discovery stops at the repo root (no pytest
# config above this file), so without these directives pytest would
# look for tests at the repo root and find none.
testpaths = backend/tests
pythonpath = backend
asyncio_mode = auto
