fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.25
alembic>=1.13.0
xarray>=2024.1.0
pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20

# ── ERA5 ingestion (ingestion/era5) ───────────────────────────────────
cdsapi>=0.7.7,<1.0
netCDF4>=1.6,<2.0

# ── District-level raster clipping (backend/district_clip/) ────────────
# These power the two-stage (bbox -> exact-polygon) clipping pipeline
# that was lifted in from the standalone raster-dist prototype. They
# are lazy-imported by the request path so a missing wheel does not
# crash other endpoints at import time, but the new
# /districts/{id}/raster-clip endpoint requires them at request time.
rasterio>=1.3
shapely>=2.0
pyproj>=3.4
fiona>=1.9
geopandas>=1.0
rioxarray>=0.16
